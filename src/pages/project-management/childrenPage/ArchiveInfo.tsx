import { ArchiveInfoVO } from "@/types/project.types";

type ArchiveInfoProps = {
    data: ArchiveInfoVO;
}

export default function ArchiveInfo({ data }: ArchiveInfoProps) {
    return <>ArchiveInfo</>
}